import 'package:flutter/material.dart';

class FilmsItemWidget extends StatelessWidget {
  const FilmsItemWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox.expand();
  }
}
